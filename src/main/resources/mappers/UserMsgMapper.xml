<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.UserMsgMapper">

    <insert id="insertUserMsg" parameterType="UserMsg">
        insert into userMsg(writerId, recipientId, content) values(#{writerId}, #{recipientId}, #{content})
    </insert>


    <select id="selectAllMyMsgById" parameterType="string" resultType="UserMsg">
        select * from userMsg where recipientId=#{id} order by wroteAt desc
    </select>

    <select id="countMyMsgById" parameterType="string" resultType="int">
        select count(*) from userMsg where recipientId=#{id} order by wroteAt desc;
    </select>



    <delete id="deleteMsgByIdx" parameterType="map">
        delete from userMsg where idx=#{msgIdx}
    </delete>


</mapper>